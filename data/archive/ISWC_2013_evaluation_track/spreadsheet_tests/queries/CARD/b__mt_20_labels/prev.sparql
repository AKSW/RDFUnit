PREFIX dbo: <http://dbpedia.org/ontology/>
PREFIX geo: <http://www.w3.org/2003/01/geo/wgs84_pos#>

# number of label strings assigned via rdfs:label (non-distinct)
SELECT COUNT(?c) {
    ?s rdfs:label ?c.
}
